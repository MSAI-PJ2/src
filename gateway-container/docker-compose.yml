# Local api-gateway compose. Run from repository root with:
# docker compose -f gateway-container/docker-compose.yml up --build api-gateway
#
# Full local cogdist stack still requires a local cogdist service/model path, which is
# not part of this gateway-container folder.
services:
  api-gateway:
    build:
      context: .
      dockerfile: api-gateway/Dockerfile
    container_name: mlnode-api-gateway
    environment:
      KLUE_API_URL: ${KLUE_API_URL:-http://host.docker.internal:8000}
      REQUEST_TIMEOUT_SECONDS: "30"
      API_KEY_REQUIRED: "false"
      ALLOWED_ORIGINS: "http://127.0.0.1:5173,http://localhost:5173"
      CONTENT_SAFETY_ENABLED: ${CONTENT_SAFETY_ENABLED:-false}
      CONTENT_SAFETY_ENDPOINT: ${CONTENT_SAFETY_ENDPOINT:-}
      CONTENT_SAFETY_KEY: ${CONTENT_SAFETY_KEY:-}
      CONTENT_SAFETY_THRESHOLD: ${CONTENT_SAFETY_THRESHOLD:-2}
      LLM_PROVIDER: ${LLM_PROVIDER:-local}
      LOCAL_LLM_BASE_URL: ${LOCAL_LLM_BASE_URL:-http://host.docker.internal:8002/v1}
      LOCAL_LLM_MODEL: ${LOCAL_LLM_MODEL:-nemotron-3-super-no-think}
      LOCAL_LLM_API_KEY: ${LOCAL_LLM_API_KEY:-dummy}
    ports:
      - "${GATEWAY_HOST_PORT:-8080}:8080"
